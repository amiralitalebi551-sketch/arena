# TESTS.md — چی واقعاً تست شد، کجا، و نتیجه‌اش

> قانون §8: «چیزی که خودت تست نکردی رو تحویل نده.» این فایل همون اثباته.
> دو دسته تست داریم: **(الف)** چیزهایی که توی این سندباکس قابل اجرا بودن و اجرا شدن،
> **(ب)** چیزهایی که فقط روی یک ماشین با اینترنت واقعی معنا دارن (روی GitHub Actions یا
> روی خود کانتینر اجرا می‌شن).

## الف) اجراشده در همین جلسه — همه سبز

این سندباکس فقط به `github.com` / `api.github.com` / `npmjs` / `pypi` راه داره
(خروجی به اینترنت آزاد بسته‌ست)، پس باینری واقعی xray-core اینجا قابل دانلود نبود.
به‌جاش هر چیزی که **خودم نوشتم** رو با تست واقعی کوبیدم:

| تست | دستور | نتیجه |
|---|---|---|
| لینتر کانفیگ (۲۶ بررسی، شامل تله‌های §6) | `node scripts/lint-config.js` | **26/26 PASS** |
| تست واحد/یکپارچگی `front.js` | `node scripts/test-front.js` | **34/34 PASS** |
| تست زنجیره‌ی `bootstrap → entrypoint → render → front` | `bash scripts/test-bootstrap.sh` | **21/21 PASS** |
| همه‌ش با هم | `bash scripts/test-all.sh` | **81/81 PASS** |

### باگ واقعی‌ای که تست پیدا کرد (و درست شد)

`front.js` اولش هدر HTTP رو با `sock.end()` می‌فرستاد و **بعد** بدنه رو با `sock.write()`.
`end()` نیمه‌بستن جریان است، پس `write()` بعدش بی‌صدا دور ریخته می‌شد →
`/__sub` و `/__panel` و `/__ip` کد ۲۰۰ می‌دادن ولی **بدنه خالی**. یعنی لینک سابسکریپشن
کار نمی‌کرد. تست `__sub is valid base64 with 3 links` همین رو لو داد.
الان هدر+بدنه در یک `end()` واحدconcat می‌شن. → **اگر تست نمی‌نوشتم، این رو بهت می‌دادم و کار نمی‌کرد.**

### چه چیزهایی دقیقاً اثبات شدن

`front.js` (با یک upstream ساختگی که هندشیک WebSocket رو جواب می‌ده):

- `GET /` → **404** · مسیر ناشناس → **404** · مسیر WS بدون upgrade → **400** · `POST` روی مسیر WS → **400**
- هندشیک درست WS روی مسیر → **101** و بعد از اون **بایت‌به‌بایت دقیق** به upstream پاس داده می‌شه
- **۲۰۰ کیلوبایت** payload یک‌جا → اکوی بایت‌به‌بایت سالم
- **۲۰ نوشتن تکه‌تکه** (هر ۲۵ms) → همه سالم؛ یعنی جریان واقعاً stream است، نه request/response
- هدر WS که روی **۴ بسته‌ی TCP** تکه‌تکه می‌رسه → هنوز ۱۰۱ (باگ رایج «peek ناقص» نداره)
- upgrade روی مسیر اشتباه → **۴۰۴** و به upstream **نمی‌ره** (پروب‌ها به xray نمی‌رسن)
- حالت TLS (برای VPS خام / Cloudflare SSL=Full) → ۴۰۴/۱۰۱/اکوی سالم
- ClientHello TLS روی پورت plain → crash نمی‌کنه · هدر ۲۰KB بدون پایان → **۴۰۰** · پروسه زنده می‌مونه
- `/__sub` → base64 معتبر، لینک‌ها با `vless://<uuid>@`، آدرس اول = Host header، آدرس دوم = IP تمیز،
  `sni`/`host` = هاست واقعی (نه IP)، `path=%2Fcf5d72f32b82`، `alpn=http%2F1.1`، `fp=chrome`
- `/__ip` با توکن اشتباه → **۴۰۴** (وجودش لو نمی‌ره) · با توکن درست → JSON با شکل درست
- `SIGTERM` → state ذخیره می‌شه

لینتر کانفیگ (`server/config.json`):

- VLESS (نه VMess) · `decryption: none` · UUID و مسیر دقیقاً همون دارایی‌های §3
- `network: ws` · `security: none` سمت سرور (چون TLS رو Cloudflare/ingress terminate می‌کنه)
- xray فقط روی `127.0.0.1` گوش می‌ده → تنها درِ باز `front.js` است
- **تله‌ی §6: هیچ `geoip:` یا `geosite:`‌ای نیست** (پس به `geoip.dat` نیاز نداره)
- به‌جاش همه‌ی CIDRهای خصوصی/لوکال **صریح** block شدن، از جمله `169.254.0.0/16`
  (متادیتای ابر) و `100.64.0.0/10` → هیچ SSRF‌ای به سمت کلاستر k8s ممکن نیست
- `freedom` اولین outbound است (مسیر پیش‌فرض) · blackhole تگ‌دار · DNS روی `UseIPv4`

زنجیره‌ی bootstrap (شبیه‌سازی دقیق کانتینر ClawCloud):

- فایل‌ها از HTTP کش می‌شن، xray کش‌شده از روی volume دوباره دانلود نمی‌شه
- `entrypoint.sh` کانفیگ رو از env رندر می‌کنه و `xray -test` قبولش می‌کنه
- **override واقعی تست شد**: با `UUID=11111111-…` و `WS_PATH=/override-path`،
  هم upstream روی مسیر جدید ۱۰۱ داد، هم مسیر پیش‌فرض قدیمی ۴۰۴ شد،
  هم لینک تولیدشده UUID و مسیر جدید رو داشت → یعنی `front.js` و `xray` هیچ‌وقت از هم واگرا نمی‌شن
- لینک تولیدشده از روی Host header: آدرس = هاست، پورت ۴۴۳، `sni`/`host` درست

## ب) چیزهایی که اینجا قابل تست نبودن (و راه‌حلشون)

| چیز | چرا اینجا نه | کی/کجا تست می‌شه |
|---|---|---|
| باینری واقعی xray-core | `objects.githubusercontent.com` از این سندباکس بسته‌ست | `ci/verify.yml` روی رانر (اینترنت آزاد) یا خودِ کانتینر |
| پروتکل §5 روی اینترنت واقعی | خروجی سندباکس allowlist داره (`google.com` → `000`) | `ci/verify.yml` + endpoint خود-آزمای کانتینر `/__verify` |
| IP خروجی ClawCloud | تا وقتی deploy نشه وجود خارجی نداره | watchdog داخل کانتینر + `ci/ip-monitor.yml` + `IP-LOG.md` |
| هندشیک از پشت Cloudflare واقعی | همین | `scripts/check-endpoint.sh` با `--resolve` روی IPهای تمیز |

**نکته‌ی مهم:** چون اکشن‌ها هنوز فعال نیستن (نگاه کن به `ci/README.md`)، خود-آزمایی
`/__verify` داخل کانتینر طوری طراحی شده که **بدون CI** هم §5 رو از سمت خروجی ثابت کنه:
۵ نمونه IP، دسترسی به ۶ سایت فیلترشده، ۵ بار `generate_204`، و تست سرعت ۱۰ مگابایتی
از `speed.cloudflare.com`. نتیجه‌اش توی `/__panel` نشون داده می‌شه و توی `/data` می‌مونه.

تنها چیزی که **هیچ** تستی نمی‌تونه از راه دور ثابت کنه: اینکه کلاینت تو (v2rayNG روی گوشیت)
واقعاً وصل می‌شه. اون رو با `scripts/check-endpoint.sh` + وصل کردن گوشی چک می‌کنیم.
